# ============================================================
# Generated from ontology/grind-077.td.json by generate_aio_asset.py
# (that file is itself generated from this device's AAS submodel --
#  see docs/ontology.md for the full AAS -> WoT -> AIO pipeline)
# Do not edit by hand -- regenerate instead.
# ============================================================
apiVersion: deviceregistry.microsoft.com/v1
kind: Asset
metadata:
  name: grind-077
  namespace: azure-iot-operations
spec:
  displayName: Legacy S7-300 Grinding Cell GRIND-077
  assetEndpointProfileRef: grind-077-mqtt-endpoint
  externalAssetId: urn:siemens:ie:asset:GRIND-077
  dataPoints:
    - name: MotorSpeed_RPM
      dataSource: "$.['DB10.DBD4']"
      dataType: Double
      unit: "RPM"
    - name: SpindleLoad_Pct
      dataSource: "$.MW102"
      dataType: Double
      unit: "%"
      # raw representation differs from the profile; Dataflow applies transform: divide_10
    - name: FeedRate_mm_min
      dataSource: "$.['DB10.DBD8']"
      dataType: Double
      unit: "mm/min"
    - name: ToolTemperature_C
      dataSource: "$.['DB10.DBD12']"
      dataType: Double
      unit: "degC"
      # raw representation differs from the profile; Dataflow applies transform: fahrenheit_to_celsius
    - name: PartCount
      dataSource: "$.['DB20.DBW0']"
      dataType: Int32
      unit: "count"
    - name: FaultCode
      dataSource: "$.['DB10.DBW20']"
      dataType: Int32
    - name: RunningStatus
      dataSource: "$.['M20.1']"
      dataType: Boolean
      # raw representation differs from the profile; Dataflow applies transform: int_to_bool
    - name: OEE_Pct
      dataSource: "$.['DB30.DBD0']"
      dataType: Double
      unit: "%"
      # raw representation differs from the profile; Dataflow applies transform: fraction_to_percent
